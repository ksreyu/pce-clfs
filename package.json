{
  "name": "pce-clfs",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npx vite build && cd ../backend && npm install && npx prisma generate && npx prisma db push --force-reset && npx tsx prisma/seed.ts",
    "start": "cd backend && npx tsx src/index.ts"
  }
}
